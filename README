Dependencies
  - Git, RVM

Installation:
  - git clone git@github.com:birula/dotfiles.git .dotfiles
  - ln -s ~/.dotfiles/bashrc .bashrc
  - ln -s ~/.dotfiles/bash_profile .bash_profile
  - ln -s ~/.dotfiles/vim/vimrc .vimrc
  - ln -s ~/.dotfiles/vim/gvimrc .gvimrc